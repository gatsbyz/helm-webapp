app:
  name: backend
  group: api
  container:
    port: 4002
  service:
    type: LoadBalancer
    port: 4002
  image:
    repository: ghcr.io/gatsbyz/node-app
    pullPolicy: Always
    tag: latest

env: true

readinessProbe:
  path: /health

livenessProbe:
  path: /health
